<%= form_for(@water_system_typology) do |f| %>
  <% if @water_system_typology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@water_system_typology.errors.count, "error") %> prohibited this water_system_typology from being saved:</h2>

      <ul>
      <% @water_system_typology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
