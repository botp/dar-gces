<%= form_for(@congressional_district) do |f| %>
  <% if @congressional_district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@congressional_district.errors.count, "error") %> prohibited this congressional_district from being saved:</h2>

      <ul>
      <% @congressional_district.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
