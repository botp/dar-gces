<%= form_for(@registering_agency) do |f| %>
  <% if @registering_agency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registering_agency.errors.count, "error") %> prohibited this registering_agency from being saved:</h2>

      <ul>
      <% @registering_agency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
