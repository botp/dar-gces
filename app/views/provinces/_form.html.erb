<%= form_for(@province) do |f| %>
  <% if @province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@province.errors.count, "error") %> prohibited this province from being saved:</h2>

      <ul>
      <% @province.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag, class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class:"form-control" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class:"form-control" %>
  </div>
  <div class="field">
    <!-- <%= f.select :region_id,options_from_collection_for_select( @regions, "id", "name", @province.region_id )%> -->

  <label for="province_region_id">Region:</label>
  <%= f.collection_select :region_id, Region.all, :id, :name, {:prompt => "Select a Region"}, {:class=>"form-control"} %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
