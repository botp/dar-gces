<%= form_for(@arc_type) do |f| %>
  <% if @arc_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arc_type.errors.count, "error") %> prohibited this arc_type from being saved:</h2>

      <ul>
      <% @arc_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
