<%= form_for(@loan_status_typology) do |f| %>
  <% if @loan_status_typology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan_status_typology.errors.count, "error") %> prohibited this loan_status_typology from being saved:</h2>

      <ul>
      <% @loan_status_typology.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
