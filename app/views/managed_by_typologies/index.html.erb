<h1>Listing managed_by_typologies</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @managed_by_typologies.each do |managed_by_typology| %>
      <tr>
        <td><%= managed_by_typology.name %></td>
        <td><%= managed_by_typology.description %></td>
        <td><%= link_to 'Show', managed_by_typology %></td>
        <td><%= link_to 'Edit', edit_managed_by_typology_path(managed_by_typology) %></td>
        <td><%= link_to 'Destroy', managed_by_typology, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Managed by typology', new_managed_by_typology_path %>
